# Frontend Environment Configuration
# When deployed to Vercel, set this to your deployed Render backend URL (e.g., https://resolveai-backend.onrender.com)
# Leave empty if frontend is served from the same origin as the backend.
VITE_API_BASE_URL=
